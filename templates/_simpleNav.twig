<nav class="top-bar" data-topbar>
    <ul class="title-area">
        <!-- Title Area -->
        <li class="name">
            <h1>
                <a href="/home.php">
                    PHOTERRAS
                </a>
            </h1>
        </li>
    </ul>

    <section class="top-bar-section">
        <!-- Right Nav Section -->
        <ul class="right">
            {% if session.loggedIn %}
            <li class="active">
                <a href="userProfile.php"><i class="user-icon"></i> Profile</a>
            </li>
            <li class="divider"></li>
            <li class="active">
                <a href="logout.php">Logout</a>
            </li>
            {% else %}
            <li class="active">
                <a href="/login.php"><i class="user-icon"></i> Login</a>
            </li>
            {% endif %}
        </ul>
    </section>
</nav>
